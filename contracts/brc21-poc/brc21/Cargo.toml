[package]
name = "brc21"
version = "0.1.0"
authors = ["Dominik Harz <dominik@interlay.io>", "Sander Bosma <sander@interlay.io>"]
edition = "2021"

[workspace]

[dependencies]
ink = { version = "4.2.0", default-features = false }

scale = { package = "parity-scale-codec", version = "3", default-features = false, features = ["derive"] }
scale-info = { version = "2.6", default-features = false, features = ["derive"], optional = true }

bitcoin = { path = "../../../crates/bitcoin", default-features = false, features = ["bitcoin-types-compat"] }
serde = { version = "1.0.136",default-features = false, features = ["derive"] }
serde_json = { version = "1.0.71", default-features = false, features = ["alloc"]}
hex = { version = "0.4.2", default-features = false }

[dev-dependencies]
ink_e2e = "4.2.0"

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "ink/std",
    "scale/std",
    "scale-info/std",
]
ink-as-dependency = []
e2e-tests = []
